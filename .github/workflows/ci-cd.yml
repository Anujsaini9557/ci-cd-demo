name: Beginner CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run build step
      run: echo "Build successful"

    - name: Run test step
      run: echo "Tests passed"

    - name: Deploy step
      run: echo "Deploying application"
